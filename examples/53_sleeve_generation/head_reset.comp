#version 460
// SDF_INSERT_POINT

layout(local_size_x = 1) in;

layout(std430, set = 0, binding = 4) buffer CounterBuffer { uint counts[]; };
layout(std430, set = 0, binding = 5) buffer IndirectBuffer { uint cmd[]; };

void main() {
    for (int i=0; i<16; i++) counts[i] = 0;
    for (int i=0; i<5; i++) cmd[i] = 0;
}
